import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gnaweparfait.weatherapp',
  appName: 'weather-app',
  webDir: 'dist'
};

export default config;
